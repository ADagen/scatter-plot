import React from 'react';
import { PlotPoints } from '../../../dataLayer/RootState';

export type PointsProps = {
    points: PlotPoints;
};

export const Points: React.FC<PointsProps> = ({
    points,
}) => {

    return (

    );
};